import 'package:flutter/material.dart';

class AppColors {
  // Main theme color
  static const Color primary = Color.fromARGB(255, 1, 110, 4); // Green

  // Amber colors
  static const Color amber = Colors.amber;
  static const Color amberAccent = Colors.amberAccent;

  // Button color (same as primary in this case)
  static const Color button = Color.fromARGB(255, 1, 110, 4); // Green
  static const Color logininiconscolors =
      Color.fromARGB(255, 1, 110, 4); // Green

  // Additional colors can be defined here
  static const Color background = Colors.white;
  static const Color text = Colors.black;
  static const Color error = Colors.red;
  static const Color success = Colors.green;
  static Color grey = Colors.grey.shade300;
}
